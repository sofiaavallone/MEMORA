services:
  db:
    image: postgres:15
    container_name: gemini_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: gemini_app
    ports:
      - "5432:5432"